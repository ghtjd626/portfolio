/**
 * @lore/ui — 유니버설 디자인 시스템(ADR-0005, ux-principles.md).
 *
 * Phase 0에서는 자리만 잡는다. Tamagui 토큰/테마와 "FieldDef → 입력 컴포넌트"
 * 동적 폼 렌더러는 Phase 2–3에서 채운다.
 * 웹(apps/web)과 모바일(apps/mobile)이 이 한 패키지를 공유한다.
 */
export const UI_PLACEHOLDER = true;
